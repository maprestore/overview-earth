{
  "name": "overview-earth",
  "private": true,
  "version": "2.3.0",
  "description": "A source-aware global signals console with an evidence-bound planetary copilot.",
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "validate:replay": "node scripts/validate-replay.mjs",
    "test": "npm run check && npm run validate:replay"
  }
}
